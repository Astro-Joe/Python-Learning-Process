# Modules are functions stored in another file and are imported to keep the main script organized and less messy.

import returning_a_list as rt_list # rt_list ia an alias used to represent the module.e


usernames = ['joseph', 'tolulope', 'damilare', 'ifeoluwa']
rt_list.greet_users(usernames)

